export default function Settings() {
  return (
    <div>
      <p className="text-xs font-bold tracking-wider uppercase text-text-muted mb-6">Configuration</p>
      <h2 className="text-2xl font-semibold text-text">Settings</h2>
      <p className="mt-4 text-text-muted">API keys, model defaults, and system configuration.</p>
    </div>
  )
}
